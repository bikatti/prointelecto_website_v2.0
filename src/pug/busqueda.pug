include lib/_var.pug
include lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include base/_head.pug
  include base/_headstyle.pug
  +metadata("Entérate si la marca que estás usando ya fue registrada"+metaFirm,"Contacta a Prointelecto para Registro de marcas y otros servicios de Propiedad Intelectual",metaAuthor[0],"https://prointelecto.com/guia-registro-de-marca",metaType[0],metaTypeImg[0],metaOgImg[0],metaImgAlt[0],metaTwitter[1])
  meta(name="robots", content="noindex")
  meta(name="googlebot-news", content="nosnippet")
  include base/_headjs.pug

 body.-bg
  // menu
  include base/_header.pug

  // Hero
  include components/forma-guia/_hero.pug

  // Contactar
  include components/forma-guia/_contact.pug
 
  // Footer
  include base/_footer.pug

  // Google Rich Card
  +jsonOrganization()