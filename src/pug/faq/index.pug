include ../lib/_var.pug
include ../lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include ../base/_head.pug
  include ../base/_headask.pug
  +metadata("Preguntas Frecuentes de Registro de marcas, nombre y lemas comerciales"+metaFirm,"Respuestas a preguntas frecuentes que surgen con relación al Registro de marca, qué es, cómo se obtiene, en qué se diferencia de otros signos comerciales",metaAuthor[1],"https://prointelecto.com/faq/",metaType[0],metaTypeImg[0],metaOgImg[0],metaImgAlt[0],metaTwitter[1])
  meta(name="googlebot-news", content="nosnippet")

 body.o-colorIndex.-bg
  // menu
  include ../base/_header.pug

  // section
  include ../components/faq/index/_hero.pug

  // section
  include ../components/faq/index/_section.pug

  // call-to-action
  include ../components/faq/index/_cta.pug

  // Footer
  include ../base/_footer.pug

  // Google Rich Card
  +jsonOrganization()