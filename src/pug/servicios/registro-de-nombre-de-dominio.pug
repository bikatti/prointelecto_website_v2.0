include ../lib/_var.pug
include ../lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include ../base/_head.pug
  include ../base/_headask.pug
  +metadata(serviceName[4]+metaFirm,"Prointelecto Abogados especialistas en Propiedad Intelectual, Registro de Marcas y trámites de Registro de nombre de Dominio - Prointelecto",metaAuthor[0],"https://prointelecto.com/servicios/registro-de-nombre-de-dominio",metaType[0],metaTypeImg[0],metaOgImg[0],metaImgAlt[0],metaTwitter[1])
  meta(name="googlebot-news", content="nosnippet")
 
 body.-bg
  // menu
  include ../base/_header.pug

  // hero
  +breadcrumb2(linksUrl[1]+linksInternos[1],titles[1],serviceName[4])

  // Content
  include ../components/servicios/registro/_content.pug

  // Footer
  include ../base/_footer.pug

  // Google Rich Card
  +jsonOrganization()