include ../lib/_var.pug
include ../lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include ../base/_head.pug
  include ../base/_headask.pug
  +metadata(serviceName[3]+metaFirm,"Tramitación de la inscripción ante la Oficina de Registro de Marcas de: licencias, cesiones, cambios de nombre y de domicilio",metaAuthor[0],"https://prointelecto.com/servicios/inscripcion-licencias-cesiones-cambios-de-nombre-domicilio",metaType[0],metaTypeImg[0],metaOgImg[0],metaImgAlt[0],metaTwitter[1])
  meta(name="googlebot-news", content="nosnippet")
 
 body.-bg
  // menu
  include ../base/_header.pug

  // hero
  +breadcrumb2(linksUrl[1]+linksInternos[1],titles[1],serviceName[3])

  // Content
  include ../components/servicios/inscripcion/_content.pug

  // Footer
  include ../base/_footer.pug
  
  // Google Rich Card
  +jsonOrganization()