include lib/_var.pug
include lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include base/_head.pug
  include base/_headstyle.pug
  +metadata("Contactar para servicios relacionados con Propiedad Intelectual"+metaFirm,"Contacta a Prointelecto para Registro de marcas y otros servicios de Propiedad Intelectual",metaAuthor[0],"https://prointelecto.com/contactar",metaType[0],metaTypeImg[0],metaOgImg[0],metaImgAlt[0],metaTwitter[1])
  meta(name="robots", content="noindex")
  meta(name="googlebot-news", content="nosnippet")

 body.-bg
  // menu
  include base/_header.pug

  // Hero
  include components/contactar/_hero.pug

  // Contactar
  include components/contactar/_contact.pug

  // Contactar
  include components/contactar/_cta.pug
 
  // Footer
  include base/_footer.pug

  // Google Rich Card
  +jsonOrganization()